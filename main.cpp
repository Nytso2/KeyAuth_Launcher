#include "keyauth_process.h"

int main() {
    Keyauth();
    // here goes your main code, calling Ketauth will run the kayauth.h , asking for the keys, after the key is completly approved, then main will be ran.;
    return 0;
}
